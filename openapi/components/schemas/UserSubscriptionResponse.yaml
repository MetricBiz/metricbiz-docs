type: object
properties:
  usedCredit:
    type: integer
    format: int64
  subscriptionType:
    type: string
    enum:
      - TRIAL
      - BASIC
      - STANDARD
      - PREMIUM
      - CUSTOM
      - NONE
  subscriptionRenewalDate:
    type: string
    format: date-time
  totalCredit:
    type: integer
    format: int64
