get:
  summary: REtrives the list of supported currencies.
  tags:
    - currency-controller
  operationId: getAllCurrencies
  responses:
    '200':
      description: OK
      content:
        '*/*':
          schema:
            type: array
            items:
              type: string
    '400':
      description: Bad Request
      content:
        '*/*':
          schema:
            $ref: ..\components\schemas\Fault.yaml
